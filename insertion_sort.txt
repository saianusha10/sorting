def insertionsort(array):
    for i in range(1, len(array)):
        k = array[i]
        j = i - 1
        while j >= 0 and k < array[j]:
           array[j + 1] = array[j]
           j -= 1
           array[j + 1] = k
    return array
    
a = insertionsort([2,6,4,9,1])
print(a)